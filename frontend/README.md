# Word Workspace Frontend

React + TypeScript workspace shell for the ONLYOFFICE-backed DOCX app.

## Commands

```powershell
npm install
npm run dev
npm run lint
npm run build
```

## Environment

Copy `.env.example` to `.env` and set:

- `VITE_API_BASE_URL`
